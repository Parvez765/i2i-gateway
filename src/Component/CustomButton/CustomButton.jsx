import React from 'react';
import Button from '../Button';
import './CustomButton.css'

const CustomButton = () => {
    return (
        <Button>
            
        </Button>
    );
};

export default CustomButton;